# 本机验证 · 2026-09-16

- 使用已有 Swift 6.2.3 / Apple Command Line Tools；未安装任何依赖或环境。
- Release 构建成功；Info.plist 与 ad-hoc 签名验证通过。
- 无 XCTest 依赖的 RestCoreChecks：4 个计时场景通过，包括首次输入、阅读计时、短/长锁屏、休息结束、延迟回调。
- 真实启动应用的 4 秒烟雾检查：两块显示器，各有一个可见遮罩窗口（1470×956、1920×1080），窗口级别 1000。
- 防空闲睡眠申请成功；并行后台进程在整个预览期间持续计数，共 45 次心跳。
- 自动结束后窗口数为 0，电源申请已释放，presentationOptions 恢复。
- 普通菜单栏实例已通过 LaunchServices 启动，并确认进程存活。

限制：Computer Use 界面读取连续超时，因此未完成视觉截图验收、长按解除的真实鼠标操作、Mission Control/全屏应用/显示器热插拔的交互验证。窗口自检不等于这些交互均已验证。锁屏计时由核心场景验证，未实际锁定用户会话。后台验证只证明测试进程连续运行，不保证 GUI 自动化或全部第三方程序不受影响。


## 1.0.1 自动熄屏计时修复

发现：原版本没有订阅 NSWorkspace.screensDidSleepNotification，接电时仅屏幕熄灭、电脑继续运行的路径仍会累计使用时间；分布式锁屏通知不是覆盖所有离开场景的可靠唯一来源。本机接电设置为 displaysleep=3、sleep=0。电源日志能证明屏幕曾长时间关闭，旧应用没有事件日志，无法回溯确认当时锁屏通知是否送达。

通过应用真实注册的 NSWorkspace 通知中心、实际 tick 路径和可注入时钟复现：10 分钟屏幕休眠前 elapsed=30，休眠后 elapsed=630，唤醒后仍是 630（FAIL）。修复后分别为 30、0、0（PASS）。这里只向当前进程的通知中心注入事件，不会让真实屏幕熄灭。

修复包含屏幕休眠/唤醒监听、CGDisplayIsAsleep 和控制台会话状态查询兜底；系统睡眠、会话离开、屏幕休眠分别记录；锁屏分布式通知改为 deliverImmediately；达到休息时长即清零，无需等到唤醒。

验证：RestCoreChecks 的 4 个原计时场景通过；RestPause --activity-check 的 8 项检查通过，包括十分钟熄屏、短暂熄屏、丢失事件的状态查询兜底、系统唤醒仍会话离开、解锁仍屏幕休眠、长时间锁屏和解锁后的进度。事件注入与系统状态样本覆盖应用处理路径；没有主动锁定用户会话，不等于真实锁屏事件送达已端到端验收。


后续按用户澄清加入无输入识别：连续无输入 60 秒暂停，达到配置的休息时长即重置；菜单栏显示“空闲”。同一实际 tick 路径新增 4 项检查：未锁屏的空闲暂停、5 分钟无操作清零、首次输入从零开始、恢复正常累计。共 12 项应用级检查。无输入是启发式判断，不等同于确认用户物理离席。

## 1.1.0 内置休息音乐

- 从 Kevin MacLeod 作者官网取得 Clear Air、Pepper’s Theme、Meditation Impromptu 01、Avec Soin 的完整音频；来源、作者、CC BY 4.0 许可及处理说明均随应用打包在 Music/Credits.txt。
- 转为 AAC 160 kbps，4 首共 15,740,931 字节（约 15 MiB）。转码后完整解码检测通过，实测综合响度为 -23.01 至 -23.02 LUFS，最高真峰值 -2.03 dBTP。
- Release 构建和签名验证通过；4 项原计时场景、12 项活动状态检查全部通过。
- 使用打包后 app 的 `--music-check`：19 项检查通过，覆盖资源解码、默认设置、关闭音乐、开启播放、随机不连播同曲、完整轮换、暂停与恢复、真实曲终回调接续、淡出至静音、结束释放、紧急停止、过期回调不重启以及设置持久化。检查使用独立偏好域。
- 真实 4 秒遮罩烟雾检查：两块屏幕分别出现窗口，单一播放器播放 Clear Air；结束后窗口为 0、防睡眠申请释放、presentation 恢复、播放器停止并释放。
- 播放验证依据本机 AVAudioPlayer 的运行状态、时间结束回调和音量值；选曲依据作者的乐器与风格说明，不代表已完成主观听感评审。锁屏／睡眠音频暂停使用状态注入检查，未主动锁定或休眠用户电脑。

## 1.2.0 观影模式

- 核心计时检查共 8 项通过：新增无输入观影持续计时且不自动休息、延后 10 分钟保留累计并在离开时暂停、长时间离开清除旧提醒、手动休息及切回普通模式。
- 打包 app 的 `--viewing-check` 共 18 项通过：真实提醒窗口可见且无遮罩／音乐，菜单栏标识正确，配置跨 Space 和全屏辅助窗口，出现后前台进程不变且窗口不取得键盘焦点，不重复创建提醒，两个原生按钮 action 正确，延后期间不提前提醒，锁屏通知立即隐藏提醒，短暂解锁恢复提醒，点击开始进入双屏遮罩，结束及预览恢复正确，菜单切换保留累计。
- 12 项原活动状态检查、19 项音乐检查全部通过；Release 构建及应用签名验证通过。
- 使用真实 NSPanel 的内容视图缓存检查了原生布局，文字及两个按钮完整可见。系统窗口截图返回 `could not create image from window`，因此该图属于原生视图渲染，不是桌面合成截图。
- 观影检查使用可注入时钟和锁屏通知，按钮通过 `performClick` 触发；未主动锁定用户会话，也未逐一实测不同播放器的原生全屏／独占全屏。全屏部分验证的是窗口配置，不代表所有播放器都已验收。
- 观影模式不持久化，重启默认普通模式；切回普通模式保留累计，达到阈值时恢复普通模式的自动休息行为。

## 1.2.1 菜单分组与观影提醒样式

- 菜单分为使用模式、休息、时长设置、音乐四组，退出单独放置；原有 action、子菜单与选中状态保留。
- 提醒与遮罩共享背景和强调色；提醒使用叶子标识、动态观看分钟数、标题说明及两个胶囊按钮。仍为不抢焦点的原生 NSPanel / NSButton。
- Release 构建、签名验证和 18 项观影应用检查通过，包含两个按钮 action、前台焦点、延后提醒和进入遮罩流程。
- 已检查原生内容视图渲染图（`.build/previews/viewing-reminder.png`），文字、间距、图标与按钮完整。系统窗口截图仍不可用，图像是视图渲染而非桌面合成截图。

## 1.2.2 空闲期间继续累计

- 普通模式空闲满 1 分钟仅改变菜单栏状态，不暂停累计；连续空闲达到配置的休息时长直接清零，后续空闲保持为零，首次输入重新开始。休眠、锁屏和观影规则不变。
- 核心检查共 11 项通过：覆盖 1 至 299 秒空闲持续累计、300 秒边界清零、持续空闲与恢复输入、休息时长配置为 3 分钟时的边界、短暂空闲恢复保留累计，以及空闲清零前达到使用阈值仍进入普通遮罩。
- 14 项应用活动检查与 18 项观影检查通过；确认「空闲」菜单状态与继续累计同时成立。Release 构建、签名验证通过。

## 1.2.3 CPU 优化 · 2026-09-17

- 旧版安装进程连续采样为 4%～5.1% CPU，另一个 10 秒 CPU 累计时间差测量为 5.09%。5 秒采样栈出现菜单栏按钮绘制、图标绘制和 NSStatusItem 副本更新；真实 tick 每 0.25 秒无条件设置相同 title。
- 新增 `--performance-check`，通过 KVO 观察真实 NSStatusBarButton.title setter，通过 Combine 观察真实倒计时发布。修复前，40 次 tick 产生 40 次重复 title 写入、120 次空闲查询，同秒倒计时重复发布；修复后为 0 次重复 title 写入、40 次空闲查询，同秒不重复发布，分钟和秒边界仍正确更新。
- 后台定时器降到每秒一次，休息时维持 0.25 秒并设置 10% 容差；结束休息恢复每秒一次。屏幕/会话状态兜底查询仍每轮执行，系统通知监听保留。观影提醒仅在分钟文本变化时赋值。
- Release 包通过核心计时 11 项、活动检查 14 项、音乐检查 19 项、观影检查 18 项、性能检查 9 项，共 71 项；编译无新增警告，签名校验通过。后台/休息/结束休息的定时器间隔也由性能检查覆盖。
- 已安装到 `/Applications/歇一会.app` 并通过 LaunchServices 重启，版本 1.2.3 (8)，已安装二进制与 dist 包 SHA-256 一致。旧版备份在 `.build/cpu-diagnosis/歇一会-1.2.2.app`。
- 新版正常后台运行的连续三个 10 秒窗口（使用 ps 累计 CPU 时间差 / 实际墙钟时间）为 0.30%、0.00%、0.10%；0.00% 受累计时间读数精度限制，并不表示绝对零消耗。随后 5 秒采样栈未再捕获原有菜单栏重复绘制热点。
- 上述 CPU 数据是当前机器的短时观测，不代表所有运行场景；本轮未重复执行真实锁屏、长时间观影和紧急解除的人工操作，相关逻辑由应用检查覆盖。

## 1.2.4 休息预告样式 · 2026-09-17

- 普通模式的一分钟休息预告改为与观影提醒一致的 400 pt 宽深绿色圆角卡片，共用背景绘制和不抢焦点的面板类；采用浅绿叶子、24 pt 标题和 13 pt 灰白说明，保留原文案和计时触发逻辑。
- Release 构建及签名验证通过，18 项既有观影检查全部通过。
- 通过实际预告内容视图生成并检查 `.build/previews/warning-reminder.png`，确认文字、图标和留白完整。这是原生视图渲染，不是桌面合成截图；观影提醒的桌面截图接口仍不可用。
- 已更新 `/Applications/歇一会.app` 为 1.2.4 (9)，签名验证通过，安装后二进制与 dist 的 SHA-256 一致；通过 LaunchServices 重启并确认进程存活。旧包保存在 `.build/style-backups/歇一会-1.2.3.app`。

## 1.2.5 暂停后恢复计时 · 2026-09-17

- 通过 `swift run RestPause --pause-check` 复现：实际菜单 selector 在暂停及继续时均调用 `session.reset()`，导致累计 120 秒后进度丢失。修复前保留进度、恢复累计和提醒恢复检查失败。
- 暂停保留累计时间及观影延后提醒；继续时只重设时钟基线和离开标记，排除暂停期间的时间。恢复时重新评估一分钟预告和观影提醒。
- 第一轮修复后普通暂停／继续检查通过，但长暂停配合锁屏／熄屏通知仍失败；根因是 `recordAbsence()` 绕过启用状态直接推进 Session。现与主 tick 一样，仅在计时启用或休息尚未结束时推进，已开始的休息仍按原截止时间结束。
- Release 包通过新增暂停检查 17 项、核心计时 11 项、活动状态 14 项、观影 18 项和性能 9 项，共 69 项。构建及签名验证通过。检查通过可注入时钟、实际菜单 selector 和通知处理方法执行，未主动锁屏或熄屏用户电脑。
- 已安装并通过 LaunchServices 重启 1.2.5 (10)，确认进程存活，安装包与 dist 二进制 SHA-256 一致。旧包备份为 `.build/pause-backups/歇一会-1.2.4.app`。

## 1.2.6 菜单栏数字简化 · 2026-09-17

- 去掉菜单栏剩余分钟数后的“分”，同步更新既有性能及暂停检查中的显示断言。
- Release 构建及签名验证通过；性能检查 9 项、暂停检查 17 项通过。
- 已安装到 `/Applications/歇一会.app` 并通过 LaunchServices 重启，版本 1.2.6 (11)，进程存活，安装后二进制与 dist 的 SHA-256 一致。旧包备份在 `.build/status-backups/歇一会-1.2.5.app`。


## 1.2.7 iPhone 专注模式联动 · 2026-09-17

- 创建本机快捷指令“歇一会-开始”和“歇一会-结束”。开始指令显式解析本次结束时间，开启对应模式直到该时间；结束指令仅在当前模式为“歇一会”时关闭。移除不必要的专注模式输出动作后，后台进程调用及真实遮罩测试不再超时。
- 新增异步串行调用模块：遮罩与紧急解除立即执行；结束排在已启动的开始指令之后；重复清理只执行一次；未启动的过期请求跳过。退出应用等待队列完成，每次调用有 15 秒超时和 1 秒终止宽限。正常使用依赖系统快捷指令，失败不阻止本机休息；到期关闭为异常退出提供恢复边界。
- 核心计时 11 项、专注模式检查 7 项、紧急解除 11 项、观影 18 项、性能 9 项全部通过，共 56 项。专注模式检查使用注入执行器，验证调用顺序、截止时间、取消、重复清理和即时解除，不将它当作手机同步证据。
- 安装包两次真实 35 秒／30 秒遮罩测试：双屏窗口数为 2，保持唤醒与音乐播放正常，结束窗口数为 0，睡眠断言释放，系统呈现选项恢复。系统日志记录本次截止时间，快捷指令进程正常退出。iPhone 镜像观察到地球壁纸切换为已绑定的休息照片，并在结束后恢复地球壁纸；跨设备同步有短暂延迟。
- iPhone 已允许所有人来电，App 通知例外为信息、微信、飞书。未拨打真实电话或触发闹钟；系统时钟闹钟不受专注模式静音规则影响。未配置强制禁用手机 App。
- Release 构建和签名验证通过，已安装 1.2.7 (12)，安装二进制与 dist 的 SHA-256 一致。旧包保存在 `.build/focus-backups/歇一会-1.2.6.app`。


## 1.2.8 手机联动开关与开机启动 · 2026-09-17

- 菜单新增“设置”组，包含“手机联动”“开机启动”。手机联动默认关闭并持久化；开启时异步读取系统快捷指令列表，只认可完全匹配的“歇一会-开始”“歇一会-结束”。齐全直接开启，无额外确认；缺少指令或读取失败时提示并保持关闭。不内置或导入指令，不直接读取专注模式及跨设备共享设置。
- 关闭联动时仍排队执行当前联动休息的结束指令，本机遮罩与计时不受影响；执行失败或超时在本机遮罩结束后提醒，每轮休息最多提示一次，退出过程中不显示该提醒。
- 开机启动使用 SMAppService.mainApp 注册／取消登录项，不单独保存一个可能与系统脱节的布尔值。菜单打开时刷新实际系统状态；需要允许的状态显示横线，并提示打开系统登录项设置。
- 新增设置检查 15 项，通过实际菜单 action 验证开关、偏好保存、单次并发检查、缺少指令、系统读取失败、名称匹配、登录项开关、外部状态刷新、待允许和注册失败。专注模式检查扩充到 9 项，新增联动中关闭和失败回调检查。另有紧急解除 11 项、观影 18 项、性能 9 项、核心计时 11 项通过，共 73 项。
- 从已安装的 1.2.8 应用包执行额外系统集成检查：真实快捷指令列表能识别本机两个指令；SMAppService 实际注册后状态为 enabled，取消后恢复未开启，共 3 项通过。手机偏好使用临时 suite，登录项测试后恢复关闭；未重启系统验证真实登录启动。
- Release 构建、签名和安装包 SHA-256 一致性检查通过。已安装 1.2.8 (13)，保留本机原有手机联动开启状态，开机启动保持关闭；旧包在 `.build/settings-backups/歇一会-1.2.7.app`。设置菜单由原生 NSMenu 与菜单 action 检查验证，未取得桌面菜单截图。
